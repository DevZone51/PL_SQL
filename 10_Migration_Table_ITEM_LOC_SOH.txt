begin
DBMS_OUTPUT.PUT_LINE('Result: ' || STOCK_SNAPSHOT_BULK.SNAPSHOT_SOH_BULK());  -- 0.47s 100K registros / 0.01s 100 registros
end;

begin
DBMS_OUTPUT.PUT_LINE('Result: ' || STOCK_SNAPSHOT.SNAPSHOT_SOH()); --0.28s 100K registros / 0.04s 100 registros
end;


TRUNCATE TABLE ITEM_LOC_SOH_EOD;

SELECT COUNT(1) FROM ITEM_LOC_SOH_EOD;

SELECT LOC, COUNT(1) FROM ITEM_LOC_SOH_EOD GROUP BY LOC;
